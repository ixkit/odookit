
 root(ComponentNode)
    app(owl.App){
        Root: WebClient
        env:{
            bus: EventBus
            services:[
                tooltip: //register js service
            ]
        }
        rawTemplates:
    }